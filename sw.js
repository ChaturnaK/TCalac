// Define repository path
const GHPATH = '/TCalac';

// Define cache name and version
const APP_PREFIX = 'tcalac_';
const VERSION = 'version_01';

// Define files to cache
const URLS = [
  `${GHPATH}/`,
  `${GHPATH}/index.html`,
  `${GHPATH}/icons.png`